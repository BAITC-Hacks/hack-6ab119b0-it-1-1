# Скопируйте в .env и подставьте свои значения. Сам .env в репозиторий не попадает.
# Ключ нужен только для необязательного текстового отчёта (explain.py).
# На решения агента он не влияет: стратегия считается детерминированно.
OPENAI_API_KEY=
